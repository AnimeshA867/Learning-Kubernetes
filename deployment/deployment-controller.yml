apiVersion: apps/v1
kind: Deployment
metadata:
  name: helloworld-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: helloworld
  template:
    metadata:
      labels:
        app: helloworld
    spec:
      containers:
      - name: k8s-demo
        image: demoapp:2
        imagePullPolicy: Never
        ports:
        - name: nodejs-port
          containerPort: 3000
      nodeSelector:
        name: node1
          